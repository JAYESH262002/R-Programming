#Types of Variables

#Integer
x<-6L #by default in R programming take values of number as double, so we need to add L at the end
typeof(x)

#Double
x2<-7
typeof(x2)

#Complex
x3<- 3+2i
typeof(x3)

#Character
h<-'H'
typeof(h)

#Logical gates(Boolean)
x4<-T
typeof(x4)

x5<-TRUE
typeof(x5)