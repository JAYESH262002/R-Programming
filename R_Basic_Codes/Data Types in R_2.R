x1<-7
x2<-14

x3<-x1+x2
x3

x4<-4L #stored Integer 4

sqrt_var<-sqrt(x4) #Square root of 4
sqrt_var

#Appending (in R known as Paste)
s1<-'Ram Ram'
s2<-'Bhai'
salutation<-paste(s1, s2) # extra space will be added at the end
salutation